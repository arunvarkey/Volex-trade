<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions.
         Only what the app actually uses. Two were dropped:

         REQUEST_IGNORE_BATTERY_OPTIMIZATIONS is restricted by Google Play to
         a narrow set of use cases (alarms, VoIP, device administration); a
         trading simulator is not one of them, and requesting it is a common
         cause of policy rejection.

         FOREGROUND_SERVICE and FOREGROUND_SERVICE_DATA_SYNC backed a
         flutter_background_service worker whose Dart side was entirely
         commented out, so no foreground service ever started. Declaring a
         foreground service type obliges you to justify it in a Play
         declaration form and shows up in the store listing's permission
         list — all for a service that did not exist. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Billing is real: SubscriptionService uses the in_app_purchase plugin
         (queryProductDetails, purchaseStream). Biometrics back the app lock;
         USE_FINGERPRINT is deprecated from API 28 but still needed at our
         minSdk of 26. -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:label="Volex Terminal"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        
        <!-- A flutter_background_service BackgroundService was declared here
             with android:exported="true", which would have let any other app
             on the device start it. Nothing ever ran it — every Dart call
             site was commented out — so the service and the unused plugin are
             gone rather than left exported. -->

        <activity
            android:name="com.antigravity.volextrade.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            
            <meta-data
                android:name="com.google.firebase.messaging.default_notification_icon"
                android:resource="@mipmap/ic_launcher" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        
        <!-- Privacy Policy URL (required by Google Play) -->
        <meta-data
            android:name="privacy_policy_url"
            android:value="https://gist.github.com/arunvarkey/5d932d686c40e4c49fe4d00394752b66" />
        
        <!-- Terms of Service URL -->
        <meta-data
            android:name="terms_of_service_url"
            android:value="https://gist.github.com/arunvarkey/4c0fb1db00e081df548c1490bb878e32" />
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
