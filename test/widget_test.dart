// Boilerplate widget smoke test — skipped because the app requires full
// service initialization that a bare pumpWidget can't provide. Real coverage
// lives in test/features/ and test/unit/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Counter increments smoke test', (WidgetTester tester) async {},
      skip: true);
}
